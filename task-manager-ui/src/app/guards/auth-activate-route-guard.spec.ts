import { AuthActivateRouteGuard } from './auth-activate-route-guard';

describe('AuthActivateRouteGuard', () => {
  it('should create an instance', () => {
    expect(new AuthActivateRouteGuard()).toBeTruthy();
  });
});
